apiVersion: v1
kind: Service
metadata:
  name: hms-service
spec:
  type: NodePort      # Added missing service type
  ports:
  - port: 8085        # Combined fields into a single list item
    targetPort: 8085  # Target container port
    nodePort: 30008   # External port exposed on nodes
  selector:
    app: hms
